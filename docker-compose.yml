services:
  mysql:
    image: mysql:8.0
    container_name: schoolweb_mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: 170522iris.
      MYSQL_DATABASE: dbTareas
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
      - ./extras/init.sql:/docker-entrypoint-initdb.d/init.sql

volumes:
  mysql_data:
