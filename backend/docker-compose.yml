services:
  mysql:
    image: mysql:8.0
    container_name: schoolweb_mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: 170522iris.
      MYSQL_DATABASE: auth_db
    command: --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
      - ./extras/microservices_init.sql:/docker-entrypoint-initdb.d/01_init.sql
      - ./extras/seed_datos_completos.sql:/docker-entrypoint-initdb.d/02_seed.sql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-p170522iris."]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  mysql_data:
